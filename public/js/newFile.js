import { i, COUNT, card_angle_list, start_angle } from './main';

for (let i = 0; i < COUNT; i++) {
	card_angle_list[i] = start_angle + i * 180 / COUNT;
	card_angle_list[i] = start_angle + i * 180 / COUNT;
}
